#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
IRIS_PROCESS_NAME="party.qwer.iris.Main"
IRIS_PROCESS_KEYWORD="app_process"
IRIS_APK_URL="https://github.com/park285/Iris/releases/latest/download/Iris.apk"
IRIS_APK_PATH="/data/local/tmp/Iris.apk"
IRIS_APK_LOCAL_FILE="Iris.apk"
IRIS_NATIVE_CORE="${IRIS_NATIVE_CORE:-off}"
IRIS_NATIVE_LIB_PATH="${IRIS_NATIVE_LIB_PATH:-/data/iris/lib/libiris_native_core.so}"
IRIS_NATIVE_LIB_LOCAL_FILE="${IRIS_NATIVE_LIB_LOCAL_FILE:-output/libiris_native_core.so}"
IRIS_SHA256_URL="${IRIS_APK_URL}.SHA256"
IRIS_ENV_FILE="${IRIS_ENV_FILE:-}"
IRIS_CONFIG_PATH="${IRIS_CONFIG_PATH:-/data/local/tmp/config.json}"
IRIS_ADB_DEVICE="${IRIS_ADB_DEVICE:-${ANDROID_SERIAL:-}}"
IRIS_ALLOW_CLEARTEXT_HTTP="${IRIS_ALLOW_CLEARTEXT_HTTP:-}"
IRIS_WEBHOOK_TRANSPORT_SECURITY_MODE="${IRIS_WEBHOOK_TRANSPORT_SECURITY_MODE:-}"
IRIS_WEBHOOK_TRANSPORT="${IRIS_WEBHOOK_TRANSPORT:-}"
IRIS_WEBHOOK_TOKEN="${IRIS_WEBHOOK_TOKEN:-}"
IRIS_BOT_TOKEN="${IRIS_BOT_TOKEN:-}"
IRIS_LOG_LEVEL="${IRIS_LOG_LEVEL:-}"
IRIS_BIND_HOST="${IRIS_BIND_HOST:-}"
IRIS_ROOM_EVENT_RETENTION_MS="${IRIS_ROOM_EVENT_RETENTION_MS:-}"
IRIS_CHATROOM_REFRESH_ENABLED="${IRIS_CHATROOM_REFRESH_ENABLED:-}"
IRIS_CHATROOM_REFRESH_INTERVAL_MS="${IRIS_CHATROOM_REFRESH_INTERVAL_MS:-}"
IRIS_CHATROOM_REFRESH_OPEN_DELAY_MS="${IRIS_CHATROOM_REFRESH_OPEN_DELAY_MS:-}"

declare -a ADB_CMD=()

load_iris_env() {
  local -a candidates=()
  local candidate
  local loaded=""

  if [[ -n "$IRIS_ENV_FILE" ]]; then
    candidates+=("$IRIS_ENV_FILE")
  fi
  candidates+=(
    "$SCRIPT_DIR/../llm/configs/iris/iris.env"
    "$SCRIPT_DIR/../arm-iris-runtime/configs/iris/iris.env"
    "/root/work/arm-iris-runtime/configs/iris/iris.env"
  )

  for candidate in "${candidates[@]}"; do
    if [[ -f "$candidate" ]]; then
      set -a
      # shellcheck source=/dev/null
      . "$candidate"
      set +a
      loaded="$candidate"
      break
    fi
  done

  if [[ -n "$IRIS_ENV_FILE" && -z "$loaded" ]]; then
    echo "Error: IRIS_ENV_FILE not found: $IRIS_ENV_FILE"
    return 1
  fi

  if [[ -n "$loaded" ]]; then
    echo "Loaded iris.env: $loaded"
  fi

  if [[ -z "$IRIS_ADB_DEVICE" && -n "${IRIS_DEVICE:-}" ]]; then
    IRIS_ADB_DEVICE="$IRIS_DEVICE"
  fi
  if [[ -z "$IRIS_WEBHOOK_TOKEN" && -n "${IRIS_SHARED_TOKEN:-}" ]]; then
    IRIS_WEBHOOK_TOKEN="$IRIS_SHARED_TOKEN"
  fi
  if [[ -z "$IRIS_BOT_TOKEN" && -n "${IRIS_SHARED_TOKEN:-}" ]]; then
    IRIS_BOT_TOKEN="$IRIS_SHARED_TOKEN"
  fi
}

check_linux_host() {
  if [[ "$(uname -s)" != "Linux" ]]; then
    echo "iris_control is supported on Linux hosts only."
    return 1
  fi
}

check_adb_installed() {
  if ! command -v adb >/dev/null 2>&1; then
    echo "adb is not installed. Please install adb and add it to your PATH."
    echo "For Debian/Ubuntu, try: sudo apt-get install adb"
    return 1
  fi
}

select_adb_device() {
  if (( ${#ADB_CMD[@]} > 0 )); then
    return 0
  fi

  if [[ -n "$IRIS_ADB_DEVICE" ]]; then
    ADB_CMD=(adb -s "$IRIS_ADB_DEVICE")
    echo "Using device from IRIS_ADB_DEVICE/ANDROID_SERIAL: $IRIS_ADB_DEVICE"
    return 0
  fi

  echo "Searching for connected devices..."
  mapfile -t devices < <(adb devices | awk '$2 == "device" {print $1}')

  case "${#devices[@]}" in
    0)
      echo "Error: No adb devices found in 'device' state."
      echo "Connect a device or run: adb connect <ip>:<port>"
      return 1
      ;;
    1)
      ADB_CMD=(adb -s "${devices[0]}")
      echo "Using device: ${devices[0]}"
      ;;
    *)
      echo "Multiple devices found. Choose one:"
      local i=1
      for device in "${devices[@]}"; do
        echo "  $i) $device"
        i=$((i + 1))
      done

      local choice
      read -r -p "Enter number (1-${#devices[@]}): " choice
      if ! [[ "$choice" =~ ^[0-9]+$ ]] || (( choice < 1 || choice > ${#devices[@]} )); then
        echo "Error: Invalid selection."
        return 1
      fi

      local device_id="${devices[$((choice - 1))]}"
      ADB_CMD=(adb -s "$device_id")
      echo "Using device: $device_id"
      ;;
  esac
}

sh_quote() {
  printf "'%s'" "$(printf '%s' "$1" | sed "s/'/'\"'\"'/g")"
}

env_assign() {
  local name="$1"
  local value="$2"
  printf '%s=%s ' "$name" "$(sh_quote "$value")"
}

get_iris_http_port() {
  "${ADB_CMD[@]}" shell "cat $(sh_quote "$IRIS_CONFIG_PATH") 2>/dev/null" |
    sed -n 's/.*"botHttpPort"[[:space:]]*:[[:space:]]*\([0-9][0-9]*\).*/\1/p' |
    head -n 1
}

get_iris_pid_by_port() {
  local port
  port="$(get_iris_http_port || true)"
  if [[ -z "$port" ]]; then
    return 0
  fi

  "${ADB_CMD[@]}" shell "su 0 sh -c 'ss -ltnp 2>/dev/null || netstat -ltnp 2>/dev/null'" |
    awk -v port=":${port}" '
      $0 ~ port && $0 ~ /app_process/ {
        for (i = 1; i <= NF; i++) {
          if ($i ~ /app_process$/) {
            split($i, parts, "/")
            print parts[1]
            exit
          }
        }
      }
    '
}

get_iris_pid() {
  local pid
  pid="$("${ADB_CMD[@]}" shell ps -f |
    grep "$IRIS_PROCESS_NAME" |
    grep "$IRIS_PROCESS_KEYWORD" |
    grep -v 'grep' |
    grep -v 'sh -c' |
    awk '{print $2}' | head -n 1 || true)"

  if [[ -n "$pid" ]]; then
    printf '%s\n' "$pid"
    return 0
  fi

  get_iris_pid_by_port || true
}

iris_status() {
  local pid
  pid="$(get_iris_pid || true)"
  if [[ -n "$pid" ]]; then
    echo "Iris is running. PID: $pid"
  else
    echo "Iris is not running."
  fi
}

build_remote_runtime_command() {
  local redirect_suffix="${1:-}"
  {
    env_assign IRIS_CONFIG_PATH "$IRIS_CONFIG_PATH"
    env_assign IRIS_ALLOW_CLEARTEXT_HTTP "$IRIS_ALLOW_CLEARTEXT_HTTP"
    env_assign IRIS_WEBHOOK_TRANSPORT_SECURITY_MODE "$IRIS_WEBHOOK_TRANSPORT_SECURITY_MODE"
    env_assign IRIS_WEBHOOK_TRANSPORT "$IRIS_WEBHOOK_TRANSPORT"
    env_assign IRIS_WEBHOOK_TOKEN "$IRIS_WEBHOOK_TOKEN"
    env_assign IRIS_BOT_TOKEN "$IRIS_BOT_TOKEN"
    env_assign IRIS_LOG_LEVEL "$IRIS_LOG_LEVEL"
    env_assign IRIS_BIND_HOST "$IRIS_BIND_HOST"
    env_assign IRIS_ROOM_EVENT_RETENTION_MS "$IRIS_ROOM_EVENT_RETENTION_MS"
    env_assign IRIS_CHATROOM_REFRESH_ENABLED "$IRIS_CHATROOM_REFRESH_ENABLED"
    env_assign IRIS_CHATROOM_REFRESH_INTERVAL_MS "$IRIS_CHATROOM_REFRESH_INTERVAL_MS"
    env_assign IRIS_CHATROOM_REFRESH_OPEN_DELAY_MS "$IRIS_CHATROOM_REFRESH_OPEN_DELAY_MS"
    env_assign IRIS_NATIVE_CORE "$IRIS_NATIVE_CORE"
    env_assign IRIS_NATIVE_LIB_PATH "$IRIS_NATIVE_LIB_PATH"
    env_assign CLASSPATH "$IRIS_APK_PATH"
    printf 'app_process / party.qwer.iris.Main%s' "$redirect_suffix"
  }
}

iris_start() {
  local pid
  pid="$(get_iris_pid || true)"
  if [[ -n "$pid" ]]; then
    echo "Iris is already running."
    return 0
  fi

  echo "Starting Iris service in the background..."
  local remote_command
  remote_command="$(build_remote_runtime_command ' > /dev/null 2>&1 &')"
  "${ADB_CMD[@]}" shell "su root sh -c $(sh_quote "$remote_command")"
  sleep 2

  local new_pid
  new_pid="$(get_iris_pid || true)"
  if [[ -n "$new_pid" ]]; then
    echo "Iris service started. PID: $new_pid"
  else
    echo "Failed to start Iris service."
    return 1
  fi
}

iris_start_foreground() {
  local pid
  pid="$(get_iris_pid || true)"
  if [[ -n "$pid" ]]; then
    echo "Iris is already running (PID: $pid). Stop it first with '$0 stop'."
    return 1
  fi

  echo "Starting Iris in foreground..."
  echo "Press Ctrl+C to stop the foreground adb session."
  local remote_command
  remote_command="$(build_remote_runtime_command)"
  "${ADB_CMD[@]}" shell "su root sh -c $(sh_quote "$remote_command")"
}

iris_stop() {
  local pid
  pid="$(get_iris_pid || true)"
  if [[ -z "$pid" ]]; then
    echo "Iris is not running."
    return 0
  fi

  echo "Stopping Iris service..."
  "${ADB_CMD[@]}" shell "su root sh -c 'kill -s SIGTERM $pid'" >/dev/null 2>&1 || true
  for _ in 1 2 3 4 5; do
    sleep 1
    if [[ -z "$(get_iris_pid || true)" ]]; then
      echo "Iris service stopped gracefully."
      return 0
    fi
  done

  echo "SIGTERM timed out; forcing SIGKILL..."
  "${ADB_CMD[@]}" shell "su root sh -c 'kill -s SIGKILL $pid'" >/dev/null 2>&1 || true
  sleep 1
  if [[ -z "$(get_iris_pid || true)" ]]; then
    echo "Iris service stopped after SIGKILL fallback."
  else
    echo "Failed to stop Iris service (PID: $pid may still be running)."
    return 1
  fi
}

download_file() {
  local url="$1"
  local output="$2"
  if command -v curl >/dev/null 2>&1; then
    curl -fsSL -o "$output" "$url"
  elif command -v wget >/dev/null 2>&1; then
    wget -O "$output" "$url"
  else
    echo "Error: curl or wget is required."
    return 1
  fi
}

verify_sha256_if_available() {
  local apk_file="$1"
  local sha256_file="$2"

  if [[ ! -f "$sha256_file" ]]; then
    echo "Warning: SHA-256 file missing. Skipping checksum verification."
    return 0
  fi

  if ! command -v sha256sum >/dev/null 2>&1; then
    echo "Warning: sha256sum is not installed. Skipping checksum verification."
    return 0
  fi

  local downloaded_sha256
  downloaded_sha256="$(awk '{print $1}' "$sha256_file")"
  if ! [[ "$downloaded_sha256" =~ ^[0-9a-fA-F]{64}$ ]]; then
    echo "Invalid SHA-256 checksum format."
    return 1
  fi
  local calculated_sha256
  calculated_sha256="$(sha256sum "$apk_file" | awk '{print $1}')"

  if [[ "${downloaded_sha256,,}" != "${calculated_sha256,,}" ]]; then
    echo "SHA-256 checksum mismatch! Downloaded file is corrupted."
    return 1
  fi

  echo "SHA-256 checksum verification passed."
}

iris_install() {
  trap cleanup_install_artifacts RETURN

  echo "Downloading Iris.apk..."
  download_file "$IRIS_APK_URL" "$IRIS_APK_LOCAL_FILE"

  echo "Downloading SHA-256 checksum..."
  if ! download_file "$IRIS_SHA256_URL" "$IRIS_APK_LOCAL_FILE.SHA256"; then
    echo "Warning: Failed to download SHA-256 checksum. Continuing without checksum verification."
  fi

  verify_sha256_if_available "$IRIS_APK_LOCAL_FILE" "$IRIS_APK_LOCAL_FILE.SHA256"

  echo "Pushing Iris.apk to device..."
  "${ADB_CMD[@]}" push "$IRIS_APK_LOCAL_FILE" "$IRIS_APK_PATH"

  if [[ -f "$IRIS_NATIVE_LIB_LOCAL_FILE" ]]; then
    local native_dir
    native_dir="$(dirname "$IRIS_NATIVE_LIB_PATH")"
    echo "Pushing native library to device: $IRIS_NATIVE_LIB_PATH"
    "${ADB_CMD[@]}" shell "su root sh -c $(sh_quote "mkdir -p $(sh_quote "$native_dir")")"
    "${ADB_CMD[@]}" push "$IRIS_NATIVE_LIB_LOCAL_FILE" "$IRIS_NATIVE_LIB_PATH"
    "${ADB_CMD[@]}" shell "su root sh -c $(sh_quote "chmod 644 $(sh_quote "$IRIS_NATIVE_LIB_PATH")")"
  else
    echo "Skipping native library push; local file not found: $IRIS_NATIVE_LIB_LOCAL_FILE"
  fi

  echo "Verifying installation..."
  if "${ADB_CMD[@]}" shell "ls $IRIS_APK_PATH" >/dev/null 2>&1; then
    echo "Installation completed successfully."
  else
    echo "Installation verification failed. File might not be at $IRIS_APK_PATH."
    return 1
  fi
}

cleanup_install_artifacts() {
  rm -f "$IRIS_APK_LOCAL_FILE" "$IRIS_APK_LOCAL_FILE.SHA256"
}

main() {
  load_iris_env || exit 1
  check_linux_host || exit 1

  case "${1:-}" in
    status|start|stop|install)
      check_adb_installed || exit 1
      select_adb_device || exit 1
      ;;
  esac

  case "${1:-}" in
    status)
      iris_status
      ;;
    start)
      if [[ "${2:-}" == "-v" || "${2:-}" == "--verbose" ]]; then
        iris_start_foreground
      else
        iris_start
      fi
      ;;
    stop)
      iris_stop
      ;;
    install)
      iris_install
      ;;
    *)
      echo "Usage: $0 {status|start [-v|--verbose]|stop|install}"
      exit 1
      ;;
  esac
}

main "$@"
